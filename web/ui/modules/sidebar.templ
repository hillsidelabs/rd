package modules

# Create a sidebar templ template module that uses tailwind and aside tag. It should then use a nav for each item. The items should be  Infrastructure, Networking, and Storage. At the bottom, should be a profile buttom that goes to the user's setting page. AI!
templ Sidebar() {


}
