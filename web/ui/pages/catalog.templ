package pages

import (
	"github.com/axzilla/templui/components"
	"github.com/hillsidelabs/rd/web/ui/layouts"
)

templ Catalog(catalog *infra.Catalog) {
	@layouts.BaseLayout() {
					# List the available hosts based on the catalog. Use a Card from the templui project https://templui.io/docs/components/card to render each host with its information. AI!
	}
}
