package layouts

import (
	"github.com/hillsidelabs/rd/web/ui/modules"
)

# Add a two column layout where there is a sidebar on the left side. This should use the modules.Sidebar and then a content area to the right. The combination of both should fill the page. AI!
templ SidebarWithContent() {
}
